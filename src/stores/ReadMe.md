absence store
admin store
app store (responsible for the app state)
attendance store
auth store
certification store
contract store
expense store
layout store
payroll store
permissions store (admin only)
roles store (admin only )
user store